/**
 * Tebex Headless API
 * The headless API is designed for implementing your own store frontend with the data of your store. You are able to call the Headless API directly from a web browser (such as within an SPA), from a backend server, or in-game GUIs.
 *
 * The version of the OpenAPI document: 2.0.1
 * Contact: tebex-integrations@overwolf.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Players from './Players';

/**
 * The ServerStatusData model module.
 * @module TebexHeadless/model/ServerStatusData
 * @version 2.0.1
 */
class ServerStatusData {
    /**
     * Constructs a new <code>ServerStatusData</code>.
     * @alias module:TebexHeadless/model/ServerStatusData
     * @param header {String} 
     * @param hostname {String} 
     * @param port {Number} 
     * @param online {Boolean} 
     * @param players {module:TebexHeadless/model/Players} 
     */
    constructor(header, hostname, port, online, players) { 
        
        ServerStatusData.initialize(this, header, hostname, port, online, players);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, header, hostname, port, online, players) { 
        obj['header'] = header;
        obj['hostname'] = hostname;
        obj['port'] = port;
        obj['online'] = online;
        obj['players'] = players;
    }

    /**
     * Constructs a <code>ServerStatusData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:TebexHeadless/model/ServerStatusData} obj Optional instance to populate.
     * @return {module:TebexHeadless/model/ServerStatusData} The populated <code>ServerStatusData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServerStatusData();

            if (data.hasOwnProperty('header')) {
                obj['header'] = ApiClient.convertToType(data['header'], 'String');
            }
            if (data.hasOwnProperty('hostname')) {
                obj['hostname'] = ApiClient.convertToType(data['hostname'], 'String');
            }
            if (data.hasOwnProperty('port')) {
                obj['port'] = ApiClient.convertToType(data['port'], 'Number');
            }
            if (data.hasOwnProperty('online')) {
                obj['online'] = ApiClient.convertToType(data['online'], 'Boolean');
            }
            if (data.hasOwnProperty('players')) {
                obj['players'] = Players.constructFromObject(data['players']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServerStatusData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServerStatusData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ServerStatusData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['header'] && !(typeof data['header'] === 'string' || data['header'] instanceof String)) {
            throw new Error("Expected the field `header` to be a primitive type in the JSON string but got " + data['header']);
        }
        // ensure the json data is a string
        if (data['hostname'] && !(typeof data['hostname'] === 'string' || data['hostname'] instanceof String)) {
            throw new Error("Expected the field `hostname` to be a primitive type in the JSON string but got " + data['hostname']);
        }
        // validate the optional field `players`
        if (data['players']) { // data not null
          Players.validateJSON(data['players']);
        }

        return true;
    }


}

ServerStatusData.RequiredProperties = ["header", "hostname", "port", "online", "players"];

/**
 * @member {String} header
 */
ServerStatusData.prototype['header'] = undefined;

/**
 * @member {String} hostname
 */
ServerStatusData.prototype['hostname'] = undefined;

/**
 * @member {Number} port
 */
ServerStatusData.prototype['port'] = undefined;

/**
 * @member {Boolean} online
 */
ServerStatusData.prototype['online'] = undefined;

/**
 * @member {module:TebexHeadless/model/Players} players
 */
ServerStatusData.prototype['players'] = undefined;






export default ServerStatusData;

