/**
 * Tebex Headless API
 * The headless API is designed for implementing your own store frontend with the data of your store. You are able to call the Headless API directly from a web browser (such as within an SPA), from a backend server, or in-game GUIs.
 *
 * The version of the OpenAPI document: 2.0.1
 * Contact: tebex-integrations@overwolf.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RecentPayment from './RecentPayment';

/**
 * The RecentPaymentsData model module.
 * @module TebexHeadless/model/RecentPaymentsData
 * @version 2.0.1
 */
class RecentPaymentsData {
    /**
     * Constructs a new <code>RecentPaymentsData</code>.
     * @alias module:TebexHeadless/model/RecentPaymentsData
     * @param header {String} 
     * @param payments {Array.<module:TebexHeadless/model/RecentPayment>} 
     */
    constructor(header, payments) { 
        
        RecentPaymentsData.initialize(this, header, payments);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, header, payments) { 
        obj['header'] = header;
        obj['payments'] = payments;
    }

    /**
     * Constructs a <code>RecentPaymentsData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:TebexHeadless/model/RecentPaymentsData} obj Optional instance to populate.
     * @return {module:TebexHeadless/model/RecentPaymentsData} The populated <code>RecentPaymentsData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RecentPaymentsData();

            if (data.hasOwnProperty('header')) {
                obj['header'] = ApiClient.convertToType(data['header'], 'String');
            }
            if (data.hasOwnProperty('payments')) {
                obj['payments'] = ApiClient.convertToType(data['payments'], [RecentPayment]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RecentPaymentsData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RecentPaymentsData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RecentPaymentsData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['header'] && !(typeof data['header'] === 'string' || data['header'] instanceof String)) {
            throw new Error("Expected the field `header` to be a primitive type in the JSON string but got " + data['header']);
        }
        if (data['payments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['payments'])) {
                throw new Error("Expected the field `payments` to be an array in the JSON data but got " + data['payments']);
            }
            // validate the optional field `payments` (array)
            for (const item of data['payments']) {
                RecentPayment.validateJSON(item);
            };
        }

        return true;
    }


}

RecentPaymentsData.RequiredProperties = ["header", "payments"];

/**
 * @member {String} header
 */
RecentPaymentsData.prototype['header'] = undefined;

/**
 * @member {Array.<module:TebexHeadless/model/RecentPayment>} payments
 */
RecentPaymentsData.prototype['payments'] = undefined;






export default RecentPaymentsData;

