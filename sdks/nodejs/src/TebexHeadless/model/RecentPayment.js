/**
 * Tebex Headless API
 * The headless API is designed for implementing your own store frontend with the data of your store. You are able to call the Headless API directly from a web browser (such as within an SPA), from a backend server, or in-game GUIs.
 *
 * The version of the OpenAPI document: 2.0.1
 * Contact: tebex-integrations@overwolf.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RecentPaymentPackage from './RecentPaymentPackage';

/**
 * The RecentPayment model module.
 * @module TebexHeadless/model/RecentPayment
 * @version 2.0.1
 */
class RecentPayment {
    /**
     * Constructs a new <code>RecentPayment</code>.
     * @alias module:TebexHeadless/model/RecentPayment
     * @param username {String} 
     * @param usernameId {String} 
     * @param _package {module:TebexHeadless/model/RecentPaymentPackage} 
     */
    constructor(username, usernameId, _package) { 
        
        RecentPayment.initialize(this, username, usernameId, _package);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, username, usernameId, _package) { 
        obj['username'] = username;
        obj['username_id'] = usernameId;
        obj['package'] = _package;
    }

    /**
     * Constructs a <code>RecentPayment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:TebexHeadless/model/RecentPayment} obj Optional instance to populate.
     * @return {module:TebexHeadless/model/RecentPayment} The populated <code>RecentPayment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RecentPayment();

            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('username_id')) {
                obj['username_id'] = ApiClient.convertToType(data['username_id'], 'String');
            }
            if (data.hasOwnProperty('package')) {
                obj['package'] = RecentPaymentPackage.constructFromObject(data['package']);
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'Number');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RecentPayment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RecentPayment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RecentPayment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // ensure the json data is a string
        if (data['username_id'] && !(typeof data['username_id'] === 'string' || data['username_id'] instanceof String)) {
            throw new Error("Expected the field `username_id` to be a primitive type in the JSON string but got " + data['username_id']);
        }
        // validate the optional field `package`
        if (data['package']) { // data not null
          RecentPaymentPackage.validateJSON(data['package']);
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }

        return true;
    }


}

RecentPayment.RequiredProperties = ["username", "username_id", "package"];

/**
 * @member {String} username
 */
RecentPayment.prototype['username'] = undefined;

/**
 * @member {String} username_id
 */
RecentPayment.prototype['username_id'] = undefined;

/**
 * @member {module:TebexHeadless/model/RecentPaymentPackage} package
 */
RecentPayment.prototype['package'] = undefined;

/**
 * Optional, present depending on module configuration.
 * @member {Date} created_at
 */
RecentPayment.prototype['created_at'] = undefined;

/**
 * Optional, present depending on module configuration.
 * @member {Number} price
 */
RecentPayment.prototype['price'] = undefined;

/**
 * Optional, present depending on module configuration.
 * @member {String} currency
 */
RecentPayment.prototype['currency'] = undefined;






export default RecentPayment;

