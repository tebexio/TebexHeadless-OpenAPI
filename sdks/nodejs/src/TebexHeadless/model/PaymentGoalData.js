/**
 * Tebex Headless API
 * The headless API is designed for implementing your own store frontend with the data of your store. You are able to call the Headless API directly from a web browser (such as within an SPA), from a backend server, or in-game GUIs.
 *
 * The version of the OpenAPI document: 2.0.1
 * Contact: tebex-integrations@overwolf.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PaymentGoalData model module.
 * @module TebexHeadless/model/PaymentGoalData
 * @version 2.0.1
 */
class PaymentGoalData {
    /**
     * Constructs a new <code>PaymentGoalData</code>.
     * @alias module:TebexHeadless/model/PaymentGoalData
     * @param header {String} 
     * @param percentage {Number} 
     * @param barStyle {module:TebexHeadless/model/PaymentGoalData.BarStyleEnum} 
     * @param barAnimated {Boolean} 
     */
    constructor(header, percentage, barStyle, barAnimated) { 
        
        PaymentGoalData.initialize(this, header, percentage, barStyle, barAnimated);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, header, percentage, barStyle, barAnimated) { 
        obj['header'] = header;
        obj['percentage'] = percentage;
        obj['bar_style'] = barStyle;
        obj['bar_animated'] = barAnimated;
    }

    /**
     * Constructs a <code>PaymentGoalData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:TebexHeadless/model/PaymentGoalData} obj Optional instance to populate.
     * @return {module:TebexHeadless/model/PaymentGoalData} The populated <code>PaymentGoalData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentGoalData();

            if (data.hasOwnProperty('header')) {
                obj['header'] = ApiClient.convertToType(data['header'], 'String');
            }
            if (data.hasOwnProperty('percentage')) {
                obj['percentage'] = ApiClient.convertToType(data['percentage'], 'Number');
            }
            if (data.hasOwnProperty('bar_style')) {
                obj['bar_style'] = ApiClient.convertToType(data['bar_style'], 'String');
            }
            if (data.hasOwnProperty('bar_animated')) {
                obj['bar_animated'] = ApiClient.convertToType(data['bar_animated'], 'Boolean');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
            if (data.hasOwnProperty('target')) {
                obj['target'] = ApiClient.convertToType(data['target'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentGoalData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentGoalData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaymentGoalData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['header'] && !(typeof data['header'] === 'string' || data['header'] instanceof String)) {
            throw new Error("Expected the field `header` to be a primitive type in the JSON string but got " + data['header']);
        }
        // ensure the json data is a string
        if (data['bar_style'] && !(typeof data['bar_style'] === 'string' || data['bar_style'] instanceof String)) {
            throw new Error("Expected the field `bar_style` to be a primitive type in the JSON string but got " + data['bar_style']);
        }

        return true;
    }


}

PaymentGoalData.RequiredProperties = ["header", "percentage", "bar_style", "bar_animated"];

/**
 * @member {String} header
 */
PaymentGoalData.prototype['header'] = undefined;

/**
 * @member {Number} percentage
 */
PaymentGoalData.prototype['percentage'] = undefined;

/**
 * @member {module:TebexHeadless/model/PaymentGoalData.BarStyleEnum} bar_style
 */
PaymentGoalData.prototype['bar_style'] = undefined;

/**
 * @member {Boolean} bar_animated
 */
PaymentGoalData.prototype['bar_animated'] = undefined;

/**
 * Optional, present depending on module configuration.
 * @member {Number} total
 */
PaymentGoalData.prototype['total'] = undefined;

/**
 * Optional, present depending on module configuration.
 * @member {Number} target
 */
PaymentGoalData.prototype['target'] = undefined;





/**
 * Allowed values for the <code>bar_style</code> property.
 * @enum {String}
 * @readonly
 */
PaymentGoalData['BarStyleEnum'] = {

    /**
     * value: "normal"
     * @const
     */
    "normal": "normal",

    /**
     * value: "striped"
     * @const
     */
    "striped": "striped"
};



export default PaymentGoalData;

