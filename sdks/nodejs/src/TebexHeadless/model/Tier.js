/**
 * Tebex Headless API
 * The headless API is designed for implementing your own store frontend with the data of your store. You are able to call the Headless API directly from a web browser (such as within an SPA), or from a backend server, such as for in-game GUIs.
 *
 * The version of the OpenAPI document: 1.1.0
 * Contact: tebex-integrations@overwolf.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Package from './Package';
import TierPendingDowngradePackage from './TierPendingDowngradePackage';
import TierStatus from './TierStatus';

/**
 * The Tier model module.
 * @module TebexHeadless/model/Tier
 * @version 1.1.0
 */
class Tier {
    /**
     * Constructs a new <code>Tier</code>.
     * @alias module:TebexHeadless/model/Tier
     */
    constructor() { 
        
        Tier.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Tier</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:TebexHeadless/model/Tier} obj Optional instance to populate.
     * @return {module:TebexHeadless/model/Tier} The populated <code>Tier</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Tier();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('username_id')) {
                obj['username_id'] = ApiClient.convertToType(data['username_id'], 'String');
            }
            if (data.hasOwnProperty('package')) {
                obj['package'] = Package.constructFromObject(data['package']);
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
            if (data.hasOwnProperty('recurring_payment_reference')) {
                obj['recurring_payment_reference'] = ApiClient.convertToType(data['recurring_payment_reference'], 'String');
            }
            if (data.hasOwnProperty('next_payment_date')) {
                obj['next_payment_date'] = ApiClient.convertToType(data['next_payment_date'], 'Date');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = TierStatus.constructFromObject(data['status']);
            }
            if (data.hasOwnProperty('pending_downgrade_package')) {
                obj['pending_downgrade_package'] = TierPendingDowngradePackage.constructFromObject(data['pending_downgrade_package']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Tier</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Tier</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['username_id'] && !(typeof data['username_id'] === 'string' || data['username_id'] instanceof String)) {
            throw new Error("Expected the field `username_id` to be a primitive type in the JSON string but got " + data['username_id']);
        }
        // validate the optional field `package`
        if (data['package']) { // data not null
          Package.validateJSON(data['package']);
        }
        // ensure the json data is a string
        if (data['recurring_payment_reference'] && !(typeof data['recurring_payment_reference'] === 'string' || data['recurring_payment_reference'] instanceof String)) {
            throw new Error("Expected the field `recurring_payment_reference` to be a primitive type in the JSON string but got " + data['recurring_payment_reference']);
        }
        // validate the optional field `status`
        if (data['status']) { // data not null
          TierStatus.validateJSON(data['status']);
        }
        // validate the optional field `pending_downgrade_package`
        if (data['pending_downgrade_package']) { // data not null
          TierPendingDowngradePackage.validateJSON(data['pending_downgrade_package']);
        }

        return true;
    }


}



/**
 * @member {Number} id
 */
Tier.prototype['id'] = undefined;

/**
 * The date and time when the tier became active
 * @member {Date} created_at
 */
Tier.prototype['created_at'] = undefined;

/**
 * The user's username ID. For Steam, this is a numeric representation of their Steam64ID
 * @member {String} username_id
 */
Tier.prototype['username_id'] = undefined;

/**
 * @member {module:TebexHeadless/model/Package} package
 */
Tier.prototype['package'] = undefined;

/**
 * True if the tier is currently active.
 * @member {Boolean} active
 */
Tier.prototype['active'] = undefined;

/**
 * The ID of the recurring payment this tier is attached to.
 * @member {String} recurring_payment_reference
 */
Tier.prototype['recurring_payment_reference'] = undefined;

/**
 * The date and time of the next payment for this tier.
 * @member {Date} next_payment_date
 */
Tier.prototype['next_payment_date'] = undefined;

/**
 * @member {module:TebexHeadless/model/TierStatus} status
 */
Tier.prototype['status'] = undefined;

/**
 * @member {module:TebexHeadless/model/TierPendingDowngradePackage} pending_downgrade_package
 */
Tier.prototype['pending_downgrade_package'] = undefined;






export default Tier;

