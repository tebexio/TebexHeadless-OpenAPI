/**
 * Tebex Headless API
 * The headless API is designed for implementing your own store frontend with the data of your store. You are able to call the Headless API directly from a web browser (such as within an SPA), from a backend server, or in-game GUIs.
 *
 * The version of the OpenAPI document: 2.0.1
 * Contact: tebex-integrations@overwolf.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TopCustomerData model module.
 * @module TebexHeadless/model/TopCustomerData
 * @version 2.0.1
 */
class TopCustomerData {
    /**
     * Constructs a new <code>TopCustomerData</code>.
     * @alias module:TebexHeadless/model/TopCustomerData
     * @param header {String} 
     * @param username {String} 
     * @param usernameId {String} User identifier.
     */
    constructor(header, username, usernameId) { 
        
        TopCustomerData.initialize(this, header, username, usernameId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, header, username, usernameId) { 
        obj['header'] = header;
        obj['username'] = username;
        obj['username_id'] = usernameId;
    }

    /**
     * Constructs a <code>TopCustomerData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:TebexHeadless/model/TopCustomerData} obj Optional instance to populate.
     * @return {module:TebexHeadless/model/TopCustomerData} The populated <code>TopCustomerData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TopCustomerData();

            if (data.hasOwnProperty('header')) {
                obj['header'] = ApiClient.convertToType(data['header'], 'String');
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('username_id')) {
                obj['username_id'] = ApiClient.convertToType(data['username_id'], 'String');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TopCustomerData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TopCustomerData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TopCustomerData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['header'] && !(typeof data['header'] === 'string' || data['header'] instanceof String)) {
            throw new Error("Expected the field `header` to be a primitive type in the JSON string but got " + data['header']);
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // ensure the json data is a string
        if (data['username_id'] && !(typeof data['username_id'] === 'string' || data['username_id'] instanceof String)) {
            throw new Error("Expected the field `username_id` to be a primitive type in the JSON string but got " + data['username_id']);
        }

        return true;
    }


}

TopCustomerData.RequiredProperties = ["header", "username", "username_id"];

/**
 * @member {String} header
 */
TopCustomerData.prototype['header'] = undefined;

/**
 * @member {String} username
 */
TopCustomerData.prototype['username'] = undefined;

/**
 * User identifier.
 * @member {String} username_id
 */
TopCustomerData.prototype['username_id'] = undefined;

/**
 * Optional, present depending on module configuration.
 * @member {Number} total
 */
TopCustomerData.prototype['total'] = undefined;






export default TopCustomerData;

