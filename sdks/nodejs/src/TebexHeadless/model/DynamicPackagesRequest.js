/**
 * Tebex Headless API
 * The headless API is designed for implementing your own store frontend with the data of your store. You are able to call the Headless API directly from a web browser (such as within an SPA), from a backend server, or in-game GUIs.
 *
 * The version of the OpenAPI document: 2.0.1
 * Contact: tebex-integrations@overwolf.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DynamicPackageInput from './DynamicPackageInput';

/**
 * The DynamicPackagesRequest model module.
 * @module TebexHeadless/model/DynamicPackagesRequest
 * @version 2.0.1
 */
class DynamicPackagesRequest {
    /**
     * Constructs a new <code>DynamicPackagesRequest</code>.
     * @alias module:TebexHeadless/model/DynamicPackagesRequest
     * @param username {String} The username of the authenticated customer the packages are being created for.
     * @param categoryId {Number} The ID of the dynamic category to populate.
     * @param packages {Array.<module:TebexHeadless/model/DynamicPackageInput>} 
     */
    constructor(username, categoryId, packages) { 
        
        DynamicPackagesRequest.initialize(this, username, categoryId, packages);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, username, categoryId, packages) { 
        obj['username'] = username;
        obj['category_id'] = categoryId;
        obj['packages'] = packages;
    }

    /**
     * Constructs a <code>DynamicPackagesRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:TebexHeadless/model/DynamicPackagesRequest} obj Optional instance to populate.
     * @return {module:TebexHeadless/model/DynamicPackagesRequest} The populated <code>DynamicPackagesRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DynamicPackagesRequest();

            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('category_id')) {
                obj['category_id'] = ApiClient.convertToType(data['category_id'], 'Number');
            }
            if (data.hasOwnProperty('packages')) {
                obj['packages'] = ApiClient.convertToType(data['packages'], [DynamicPackageInput]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DynamicPackagesRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DynamicPackagesRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DynamicPackagesRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        if (data['packages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['packages'])) {
                throw new Error("Expected the field `packages` to be an array in the JSON data but got " + data['packages']);
            }
            // validate the optional field `packages` (array)
            for (const item of data['packages']) {
                DynamicPackageInput.validateJSON(item);
            };
        }

        return true;
    }


}

DynamicPackagesRequest.RequiredProperties = ["username", "category_id", "packages"];

/**
 * The username of the authenticated customer the packages are being created for.
 * @member {String} username
 */
DynamicPackagesRequest.prototype['username'] = undefined;

/**
 * The ID of the dynamic category to populate.
 * @member {Number} category_id
 */
DynamicPackagesRequest.prototype['category_id'] = undefined;

/**
 * @member {Array.<module:TebexHeadless/model/DynamicPackageInput>} packages
 */
DynamicPackagesRequest.prototype['packages'] = undefined;






export default DynamicPackagesRequest;

