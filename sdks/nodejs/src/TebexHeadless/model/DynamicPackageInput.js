/**
 * Tebex Headless API
 * The headless API is designed for implementing your own store frontend with the data of your store. You are able to call the Headless API directly from a web browser (such as within an SPA), from a backend server, or in-game GUIs.
 *
 * The version of the OpenAPI document: 2.0.1
 * Contact: tebex-integrations@overwolf.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DynamicPackageInput model module.
 * @module TebexHeadless/model/DynamicPackageInput
 * @version 2.0.1
 */
class DynamicPackageInput {
    /**
     * Constructs a new <code>DynamicPackageInput</code>.
     * A custom package to add to a dynamic category.
     * @alias module:TebexHeadless/model/DynamicPackageInput
     * @param name {String} 
     * @param price {Number} 
     * @param slug {String} 
     */
    constructor(name, price, slug) { 
        
        DynamicPackageInput.initialize(this, name, price, slug);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, price, slug) { 
        obj['name'] = name;
        obj['price'] = price;
        obj['slug'] = slug;
    }

    /**
     * Constructs a <code>DynamicPackageInput</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:TebexHeadless/model/DynamicPackageInput} obj Optional instance to populate.
     * @return {module:TebexHeadless/model/DynamicPackageInput} The populated <code>DynamicPackageInput</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DynamicPackageInput();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'Number');
            }
            if (data.hasOwnProperty('slug')) {
                obj['slug'] = ApiClient.convertToType(data['slug'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('image_url')) {
                obj['image_url'] = ApiClient.convertToType(data['image_url'], 'String');
            }
            if (data.hasOwnProperty('custom')) {
                obj['custom'] = ApiClient.convertToType(data['custom'], {'String': 'String'});
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DynamicPackageInput</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DynamicPackageInput</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DynamicPackageInput.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['slug'] && !(typeof data['slug'] === 'string' || data['slug'] instanceof String)) {
            throw new Error("Expected the field `slug` to be a primitive type in the JSON string but got " + data['slug']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['image_url'] && !(typeof data['image_url'] === 'string' || data['image_url'] instanceof String)) {
            throw new Error("Expected the field `image_url` to be a primitive type in the JSON string but got " + data['image_url']);
        }

        return true;
    }


}

DynamicPackageInput.RequiredProperties = ["name", "price", "slug"];

/**
 * @member {String} name
 */
DynamicPackageInput.prototype['name'] = undefined;

/**
 * @member {Number} price
 */
DynamicPackageInput.prototype['price'] = undefined;

/**
 * @member {String} slug
 */
DynamicPackageInput.prototype['slug'] = undefined;

/**
 * @member {String} description
 */
DynamicPackageInput.prototype['description'] = undefined;

/**
 * @member {String} image_url
 */
DynamicPackageInput.prototype['image_url'] = undefined;

/**
 * Arbitrary custom key/value data to associate with the package.
 * @member {Object.<String, String>} custom
 */
DynamicPackageInput.prototype['custom'] = undefined;






export default DynamicPackageInput;

